const karisoft = function ({textSize="text-5xl" }) {
    return (
        <>
            <h1 className={`${textSize} text-white font-medium" `}>
                <span className="font-bold">Kari</span>
                Soft
            </h1>
        </>
    )
}

export default karisoft;